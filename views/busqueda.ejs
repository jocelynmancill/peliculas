<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Resultados de búsqueda</title>
  <link rel="stylesheet" href="/css/style.css"> 
</head>
<body>
  <h1>Resultados de búsqueda</h1>

 
  <form action="/buscar" method="GET">
    <input type="text" name="q" placeholder="Buscar películas..." value="<%= query %>" required>
    <button type="submit">Buscar</button>
  </form>

  <% if (peliculas.length === 0) { %>
    <p>No se encontraron resultados para "<%= terminoBusqueda %>".</p>
  <% } else { %>
    <ul>
      <% peliculas.forEach(pelicula => { %>
        <li>
          <h2><%= pelicula.Título %> (<%= pelicula.Año %>)</h2>
          <p><strong>Categoría:</strong> <%= pelicula.Categoría %></p>
          <p><strong>Actores:</strong> <%= pelicula.Actores.join(', ') %></p>
          <p><strong>Sinopsis:</strong> <%= pelicula.Sinopsis %></p>
        </li>
      <% }); %>
    </ul>
  <% } %>

  <a href="/">Volver al inicio</a>
</body>
</html>
